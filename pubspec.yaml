name: agent_client_sdk
description: "Agent typed client SDK for Dart/Flutter over easy-rpc (agent.v1.AgentService)."
version: 0.17.0
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  easy_rpc:
    git:
      url: https://github.com/easy-utils/easy-rpc-dart.git
      ref: v3.0.3
  protobuf: ^6.1.0
  fixnum: ^1.1.1
